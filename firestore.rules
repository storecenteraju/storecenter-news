rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Toda leitura e gravação passa pela API autenticada do Store Center.
    // O Firebase Admin SDK do servidor ignora estas regras e usa IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
